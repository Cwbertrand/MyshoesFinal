<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/css" href="{{ asset('css/sitemap.css') }}"?>
<urlset >

    <sitemap>
        {% for url in urls %}
        <url>
            <loc>{{hostname}}{{url.loc}}</loc>
        </url>
        {% endfor %}

    </sitemap>

</urlset >